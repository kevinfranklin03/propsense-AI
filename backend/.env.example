# --- Database Configuration ---
# Local Development (SQLite):
DATABASE_URL=sqlite:///./prop_sense.db

# Production (Azure SQL):
# DATABASE_URL=mssql+pyodbc://username:password@server.database.windows.net/db_name?driver=ODBC+Driver+17+for+SQL+Server

# --- Azure IoT Configuration ---
# Connection String for the IoT Device (from Azure Portal -> IoT Hub -> Devices)
# Leave empty to use Local Simulator Mode
AZURE_IOT_CONNECTION_STRING=HostName=my-hub.azure-devices.net;DeviceId=sensor_01;SharedAccessKey=...

# --- API Security ---
# Secret key for JWT tokens (if we add login later)
SECRET_KEY=super-secret-key-change-me
